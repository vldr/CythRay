import "env"
    void log(string a)
    void log(int n)

log("Length: " + "".toArray().length)
log("Length: " + "hello".toArray().length)

for char c in "hello".toArray()
    log("" + c)

# Length: 0
# Length: 5
# h
# e
# l
# l
# o