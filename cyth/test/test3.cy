int sqrt0(int x)
    int s
    int b = 32768

    while b != 0
        int t = s + b 

        if t * t <= x
            s = t

        b = b / 2

    return s

int pow0(int base, int exp)
    int result = 1

    while exp != 0
        if exp % 2 == 1
            result = result * base
            
        exp = exp / 2
        base = base * base
    
    return result

int sqrt1(int x)
    int s
    int b = 32768

    while b != 0
        int t = s + b 

        if t * t <= x
            s = t

        b = b / 2

    return s

int pow1(int base, int exp)
    int result = 1

    while exp != 0
        if exp % 2 == 1
            result = result * base
            
        exp = exp / 2
        base = base * base
    
    return result

int sqrt2(int x)
    int s
    int b = 32768

    while b != 0
        int t = s + b 

        if t * t <= x
            s = t

        b = b / 2

    return s

int pow2(int base, int exp)
    int result = 1

    while exp != 0
        if exp % 2 == 1
            result = result * base
            
        exp = exp / 2
        base = base * base
    
    return result

int sqrt3(int x)
    int s
    int b = 32768

    while b != 0
        int t = s + b 

        if t * t <= x
            s = t

        b = b / 2

    return s

int pow3(int base, int exp)
    int result = 1

    while exp != 0
        if exp % 2 == 1
            result = result * base
            
        exp = exp / 2
        base = base * base
    
    return result

int sqrt4(int x)
    int s
    int b = 32768

    while b != 0
        int t = s + b 

        if t * t <= x
            s = t

        b = b / 2

    return s

int pow4(int base, int exp)
    int result = 1

    while exp != 0
        if exp % 2 == 1
            result = result * base
            
        exp = exp / 2
        base = base * base
    
    return result

int sqrt5(int x)
    int s
    int b = 32768

    while b != 0
        int t = s + b 

        if t * t <= x
            s = t

        b = b / 2

    return s

int pow5(int base, int exp)
    int result = 1

    while exp != 0
        if exp % 2 == 1
            result = result * base
            
        exp = exp / 2
        base = base * base
    
    return result

int sqrt6(int x)
    int s
    int b = 32768

    while b != 0
        int t = s + b 

        if t * t <= x
            s = t

        b = b / 2

    return s

int pow6(int base, int exp)
    int result = 1

    while exp != 0
        if exp % 2 == 1
            result = result * base
            
        exp = exp / 2
        base = base * base
    
    return result

int sqrt7(int x)
    int s
    int b = 32768

    while b != 0
        int t = s + b 

        if t * t <= x
            s = t

        b = b / 2

    return s

int pow7(int base, int exp)
    int result = 1

    while exp != 0
        if exp % 2 == 1
            result = result * base
            
        exp = exp / 2
        base = base * base
    
    return result

int sqrt8(int x)
    int s
    int b = 32768

    while b != 0
        int t = s + b 

        if t * t <= x
            s = t

        b = b / 2

    return s

int pow8(int base, int exp)
    int result = 1

    while exp != 0
        if exp % 2 == 1
            result = result * base
            
        exp = exp / 2
        base = base * base
    
    return result

int sqrt9(int x)
    int s
    int b = 32768

    while b != 0
        int t = s + b 

        if t * t <= x
            s = t

        b = b / 2

    return s

int pow9(int base, int exp)
    int result = 1

    while exp != 0
        if exp % 2 == 1
            result = result * base
            
        exp = exp / 2
        base = base * base
    
    return result

int sqrt10(int x)
    int s
    int b = 32768

    while b != 0
        int t = s + b 

        if t * t <= x
            s = t

        b = b / 2

    return s

int pow10(int base, int exp)
    int result = 1

    while exp != 0
        if exp % 2 == 1
            result = result * base
            
        exp = exp / 2
        base = base * base
    
    return result

int sqrt11(int x)
    int s
    int b = 32768

    while b != 0
        int t = s + b 

        if t * t <= x
            s = t

        b = b / 2

    return s

int pow11(int base, int exp)
    int result = 1

    while exp != 0
        if exp % 2 == 1
            result = result * base
            
        exp = exp / 2
        base = base * base
    
    return result

int sqrt12(int x)
    int s
    int b = 32768

    while b != 0
        int t = s + b 

        if t * t <= x
            s = t

        b = b / 2

    return s

int pow12(int base, int exp)
    int result = 1

    while exp != 0
        if exp % 2 == 1
            result = result * base
            
        exp = exp / 2
        base = base * base
    
    return result

int sqrt13(int x)
    int s
    int b = 32768

    while b != 0
        int t = s + b 

        if t * t <= x
            s = t

        b = b / 2

    return s

int pow13(int base, int exp)
    int result = 1

    while exp != 0
        if exp % 2 == 1
            result = result * base
            
        exp = exp / 2
        base = base * base
    
    return result

int sqrt14(int x)
    int s
    int b = 32768

    while b != 0
        int t = s + b 

        if t * t <= x
            s = t

        b = b / 2

    return s

int pow14(int base, int exp)
    int result = 1

    while exp != 0
        if exp % 2 == 1
            result = result * base
            
        exp = exp / 2
        base = base * base
    
    return result

int sqrt15(int x)
    int s
    int b = 32768

    while b != 0
        int t = s + b 

        if t * t <= x
            s = t

        b = b / 2

    return s

int pow15(int base, int exp)
    int result = 1

    while exp != 0
        if exp % 2 == 1
            result = result * base
            
        exp = exp / 2
        base = base * base
    
    return result

int sqrt16(int x)
    int s
    int b = 32768

    while b != 0
        int t = s + b 

        if t * t <= x
            s = t

        b = b / 2

    return s

int pow16(int base, int exp)
    int result = 1

    while exp != 0
        if exp % 2 == 1
            result = result * base
            
        exp = exp / 2
        base = base * base
    
    return result

int sqrt17(int x)
    int s
    int b = 32768

    while b != 0
        int t = s + b 

        if t * t <= x
            s = t

        b = b / 2

    return s

int pow17(int base, int exp)
    int result = 1

    while exp != 0
        if exp % 2 == 1
            result = result * base
            
        exp = exp / 2
        base = base * base
    
    return result

int sqrt18(int x)
    int s
    int b = 32768

    while b != 0
        int t = s + b 

        if t * t <= x
            s = t

        b = b / 2

    return s

int pow18(int base, int exp)
    int result = 1

    while exp != 0
        if exp % 2 == 1
            result = result * base
            
        exp = exp / 2
        base = base * base
    
    return result

int sqrt19(int x)
    int s
    int b = 32768

    while b != 0
        int t = s + b 

        if t * t <= x
            s = t

        b = b / 2

    return s

int pow19(int base, int exp)
    int result = 1

    while exp != 0
        if exp % 2 == 1
            result = result * base
            
        exp = exp / 2
        base = base * base
    
    return result

int sqrt20(int x)
    int s
    int b = 32768

    while b != 0
        int t = s + b 

        if t * t <= x
            s = t

        b = b / 2

    return s

int pow20(int base, int exp)
    int result = 1

    while exp != 0
        if exp % 2 == 1
            result = result * base
            
        exp = exp / 2
        base = base * base
    
    return result

int sqrt21(int x)
    int s
    int b = 32768

    while b != 0
        int t = s + b 

        if t * t <= x
            s = t

        b = b / 2

    return s

int pow21(int base, int exp)
    int result = 1

    while exp != 0
        if exp % 2 == 1
            result = result * base
            
        exp = exp / 2
        base = base * base
    
    return result

int sqrt22(int x)
    int s
    int b = 32768

    while b != 0
        int t = s + b 

        if t * t <= x
            s = t

        b = b / 2

    return s

int pow22(int base, int exp)
    int result = 1

    while exp != 0
        if exp % 2 == 1
            result = result * base
            
        exp = exp / 2
        base = base * base
    
    return result

int sqrt23(int x)
    int s
    int b = 32768

    while b != 0
        int t = s + b 

        if t * t <= x
            s = t

        b = b / 2

    return s

int pow23(int base, int exp)
    int result = 1

    while exp != 0
        if exp % 2 == 1
            result = result * base
            
        exp = exp / 2
        base = base * base
    
    return result

int sqrt24(int x)
    int s
    int b = 32768

    while b != 0
        int t = s + b 

        if t * t <= x
            s = t

        b = b / 2

    return s

int pow24(int base, int exp)
    int result = 1

    while exp != 0
        if exp % 2 == 1
            result = result * base
            
        exp = exp / 2
        base = base * base
    
    return result

int sqrt25(int x)
    int s
    int b = 32768

    while b != 0
        int t = s + b 

        if t * t <= x
            s = t

        b = b / 2

    return s

int pow25(int base, int exp)
    int result = 1

    while exp != 0
        if exp % 2 == 1
            result = result * base
            
        exp = exp / 2
        base = base * base
    
    return result

int sqrt26(int x)
    int s
    int b = 32768

    while b != 0
        int t = s + b 

        if t * t <= x
            s = t

        b = b / 2

    return s

int pow26(int base, int exp)
    int result = 1

    while exp != 0
        if exp % 2 == 1
            result = result * base
            
        exp = exp / 2
        base = base * base
    
    return result

int sqrt27(int x)
    int s
    int b = 32768

    while b != 0
        int t = s + b 

        if t * t <= x
            s = t

        b = b / 2

    return s

int pow27(int base, int exp)
    int result = 1

    while exp != 0
        if exp % 2 == 1
            result = result * base
            
        exp = exp / 2
        base = base * base
    
    return result

int sqrt28(int x)
    int s
    int b = 32768

    while b != 0
        int t = s + b 

        if t * t <= x
            s = t

        b = b / 2

    return s

int pow28(int base, int exp)
    int result = 1

    while exp != 0
        if exp % 2 == 1
            result = result * base
            
        exp = exp / 2
        base = base * base
    
    return result

int sqrt29(int x)
    int s
    int b = 32768

    while b != 0
        int t = s + b 

        if t * t <= x
            s = t

        b = b / 2

    return s

int pow29(int base, int exp)
    int result = 1

    while exp != 0
        if exp % 2 == 1
            result = result * base
            
        exp = exp / 2
        base = base * base
    
    return result


int sqrt30(int x)
    int s
    int b = 32768

    while b != 0
        int t = s + b 

        if t * t <= x
            s = t

        b = b / 2

    return s

int pow30(int base, int exp)
    int result = 1

    while exp != 0
        if exp % 2 == 1
            result = result * base
            
        exp = exp / 2
        base = base * base
    
    return result

int sqrt31(int x)
    int s
    int b = 32768

    while b != 0
        int t = s + b 

        if t * t <= x
            s = t

        b = b / 2

    return s